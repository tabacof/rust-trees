
[tool.maturin]
python-source = "python"
module-name = "rustrees.rustrees"

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.black]
line-length = 88

[tool.ruff]
line-length = 88
